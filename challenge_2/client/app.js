import {ajax} from 'jquery';


const App = {
    render: (event) => {
        ajax({
            method: 'GET',
            url: '/',
            data: 
            success:
            error:
        })

    }
}

